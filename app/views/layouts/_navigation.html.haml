= link_to root_url, :class => 'brand' do
  -# = render 'layouts/logo'
  = 'WORKSOFT'

.nav-collapse
  = render_navigation  :expand_all => true, :renderer => :bootstrap
  %ul.nav
    %li
      = link_to '#', :class => 'user-versions-link' do
        %span.version-support-kernel
          8.2
        %span.version-support-configs БухгалтерияПредприятия
        = fontello 'cog', 18
  -#= bootstrap_navigation :type=>'' do |nav|
    -#- nav.link_to "Пакеты", root_path, :active_nav => controller.is_a?(BundlesController)
    -#- nav.link_to 'Панель разработчика', developer_dashboard_path
    -#- nav.link_to 'Админка', admin_dashboard_path

  -#%ul.nav.pull-right
    -#- if logged_in?
      -#%li
        -#= link_to current_user, "/profile"
      -#%li
        -#= link_to 'выход', logout_path, :method => :delete
    -#- else
      -#-# %li= link_to 'вход', new_user_session_path
