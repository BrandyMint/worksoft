%ul.nav.nav-pills.kinds-nav
  - @all_kindes.each_pair do |id, tab|
    = active_link_to tab.title, apps_path({ :kind_id => id, "app_search_query"=>{"name"=>@query.name} }), :wrap_tag => :li, :active => tab.active, :class => "app-kind-#{id}"

= render 'apps/search_form'
%hr
= render 'apps/search_results'
